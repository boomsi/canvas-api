import { IRectOptions } from "./interfaces";

export const rectDefaultOptions: IRectOptions = {
    width: 100,
    height: 100,
    left: 0,
    top: 0,
    strokeStyle: 'black',
    lineWidth: 1,
    fillStyle: 'white'
}